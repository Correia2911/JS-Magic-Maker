// Define the function to concatenate a string
function addText() {
    // Use document.getElementById to access the element
    document.getElementById("output").innerHTML += " This is the added part."; 
    // Concatenates the new string to the existing content in the paragraph
}
// Call the function